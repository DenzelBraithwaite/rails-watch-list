<%= simple_form_for([list, bookmark]) do |f| %>
<%= f.input :comment, placeholder: 'Enter a short comment' %>
<%= f.association :movie %>
<%= f.association :list %>
<%= f.submit class: "btn btn-success"%>
<% end %>

<%= link_to 'back', lists_path %>
